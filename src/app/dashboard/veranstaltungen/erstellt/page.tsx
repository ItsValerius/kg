import React from 'react'

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard | Veranstaltungen | Erstellt"
};

const EventErstellt = () => {
  return (
    <div>EventErstellt</div>
  )
}

export default EventErstellt